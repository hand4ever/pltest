#include <stdio.h>
#include <string.h>
int main ()
{
    char greeting[6] = {'H', 'e', 'l', 'l', 'o', '\0'};
   // char str[80];
   // char *salt = "hello";
   // 
   // strcpy (str,"these ");
   // strcat (str,"strings ");
   // strcat (str,"are ");
   // strcat (str,"concatenated.");
   // strcat(str, salt);
   // puts (str);
    return 0;
}
